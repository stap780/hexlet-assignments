en:
  activerecord:
    models:
      # BEGIN
      post: Post
      posts: Posts
      # END
      post/comment: Comment
    attributes:
      post:
        # BEGIN
        title: Title
        body: Body
        # END
      post/comment:
        body: Body
    errors:
      models:
        post:
          attributes:
            # BEGIN
            title:
              blank: "should be filled"
            # END